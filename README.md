# Renju

## How to install dependencies

Run this command in the root directory of the project to install dependencies:

```bash
npm install
```

## How to run

Run the following command to compile TS code into JS and execute program:

```bash
npm run start
```



